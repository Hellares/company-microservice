export interface PlanLimits {
  maxSeats: number; // Número máximo de usuarios
  maxLocations: number;
  maxStorageGB: number;
  maxProductsPerLocation: number;
  maxMonthlyOrders: number;
  maxCustomers: number;
  maxImagesPerProduct: number;
}